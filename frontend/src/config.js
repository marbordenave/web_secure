const config = {
  apiUrl: process.env.REACT_APP_API_URL || "http://localhost:3000",
  parcServiceUrl: process.env.REACT_APP_PARCSERVICE_URL,
  apiKey: process.env.REACT_APP_API_KEY,
};

export default config;

// export default config;
